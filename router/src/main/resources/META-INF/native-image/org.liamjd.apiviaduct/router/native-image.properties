# Native-image build arguments contributed by the APIViaduct router library.
# This file is picked up automatically by native-image for any consumer that
# has the router library on its classpath.
#
# --enable-url-protocols=https is required because the jwks-rsa library fetches
# JWKS keys over HTTPS at runtime.
Args = --enable-url-protocols=https
